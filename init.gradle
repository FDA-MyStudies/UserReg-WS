allprojects {
        // Properties for standalone build.
        // We define these here, instead of 'gradle.properties' to avoid overwriting properties in a standard build
    ext {
        labkeyVersion = "22.6-SNAPSHOT"

        buildFromSource = true

        deployMode = "prod"
    }
}
